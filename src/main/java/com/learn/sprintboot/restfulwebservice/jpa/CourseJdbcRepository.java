/*package com.learn.sprintboot.restfulwebservice.jdbc;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.stereotype.Repository;
*/
/**
 * The Table is created using the schema.sql under src/resources
 */
/*@Repository //@Repository >>> this class talks to DB
public class CourseJdbcRepository {
    @Autowired
    private JdbcTemplate jdbcTemplate;
    private static String INSERT_QUERY = "insert into course (id, name, author) values(101,'Learn Spring Boot','in28min');";

    public void insert() {
        System.out.println(">>>>>>>>> INSERTING INTO COURSE TABLE <<<<<<<<<<<<<<");
        jdbcTemplate.update(INSERT_QUERY);
    }
}
*/